name: atflows
version: "0.1.4-beta.2"
description: "Local metadata-only AtFlows observer; preserves model and memory providers."
requires_hermes: "==0.21.5"
provides_hooks:
  - post_api_request
  - api_request_error
  - post_tool_call
